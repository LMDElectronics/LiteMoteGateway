LiteMotaGateway app:
--------------------

Falta:
 
  - cambiar el edit de msg type por todos los tipos de mensajes disponibles
  - seleccionar desde el inicio los parámetros del puerto serie (en vez de generarlos en el edit crearlos en el codigo como databindings con enums)